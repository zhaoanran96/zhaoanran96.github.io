import{_ as a,W as e,X as s,a2 as n}from"./framework-6199cc12.js";const i={},d=n(`<h1 id="redis持久化" tabindex="-1"><a class="header-anchor" href="#redis持久化" aria-hidden="true">#</a> Redis持久化</h1><h2 id="redis持久化-1" tabindex="-1"><a class="header-anchor" href="#redis持久化-1" aria-hidden="true">#</a> redis持久化</h2><p>Redis 为了保证效率，数据缓存在了内存中，但是会周期性的把更新的数据写入磁盘或者把修改操作写入追加的记录文件中，以保证数据的持久化。</p><p>Redis 的持久化策略有两种：</p><ul><li>RDB：快照形式是直接把内存中的数据保存到一个 dump 的文件中，定时保存，保存策略。</li><li>AOF：把所有的对 Redis 的服务器进行修改的命令都存到一个文件里，命令的集合。</li></ul><h2 id="rdb" tabindex="-1"><a class="header-anchor" href="#rdb" aria-hidden="true">#</a> RDB</h2><p>Redis 默认是快照 RDB 的持久化方式。</p><div class="language-java line-numbers-mode" data-ext="java"><pre class="language-java"><code>当 <span class="token class-name">Redis</span> 重启的时候，它会优先使用 <span class="token constant">AOF</span> 文件来还原数据集，因为 <span class="token constant">AOF</span> 文件保存的数据集通常比 <span class="token constant">RDB</span> 文件所保存的数据集更完整。你甚至可以关闭持久化功能，让数据只在服务器运行时存。
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>下面在来谈一谈RDB 是怎么工作的</p><ul><li><p>默认 Redis 是会以快照&quot;RDB&quot;的形式将数据持久化到磁盘的一个二进制文件 dump.rdb。</p></li><li><p>工作原理简单说一下：当 Redis 需要做持久化时，Redis 会 fork 一个子进程，子进程将数据写到磁盘上一个临时 RDB 文件中。</p></li><li><p>当子进程完成写临时文件后，将原来的 RDB 替换掉，这样的好处是可以 copy-on-write。</p></li><li><p>RDB 的优点是：这种文件非常适合用于备份：比如，你可以在最近的 24 小时内，每小时备份一次，并且在每个月的每一天也备份一个 RDB 文件。</p></li><li><p>这样的话，即使遇上问题，也可以随时将数据集还原到不同的版本。RDB 非常适合灾难恢复。</p></li><li><p>RDB 的缺点是：如果你需要尽量避免在服务器故障时丢失数据，那么RDB不合适你。</p></li><li><p>既然RDB都说了，就顺便在介绍一下AOF。</p></li></ul><h2 id="aof" tabindex="-1"><a class="header-anchor" href="#aof" aria-hidden="true">#</a> AOF</h2><p>使用 AOF 做持久化，每一个写命令都通过 write 函数追加到 appendonly.aof 中，配置方式如下：</p><div class="language-java line-numbers-mode" data-ext="java"><pre class="language-java"><code>appendfsync yes   
appendfsync always     #每次有数据修改发生时都会写入<span class="token constant">AOF</span>文件。
appendfsync everysec   #每秒钟同步一次，该策略为<span class="token constant">AOF</span>的缺省策略。

</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>AOF 可以做到全程持久化，只需要在配置中开启 appendonly yes。这样 Redis 每执行一个修改数据的命令，都会把它添加到 AOF 文件中，当 Redis 重启时，将会读取 AOF 文件进行重放，恢复到 Redis 关闭前的最后时刻。</p><h3 id="aof同步策略" tabindex="-1"><a class="header-anchor" href="#aof同步策略" aria-hidden="true">#</a> AOF同步策略</h3><p>aof缓冲区数据写入aof文件时的同步策略：</p><p>appendfsync always/eversec/no aof缓冲区数据写入aof文件时的同步策略。</p><p>涉及操作系统的write命令、fsync命令、io缓冲区：</p><p>always 一旦aof缓冲区有数据时，就调用fsync命令强制往aof文件写数据（redis性能最差，数据可控性最好） eversec （默认）一旦aof缓冲区有数据时，就调用write命令往io缓冲区写数据，同时每秒调用一次fsync命令强制将io缓冲区的数据写入到aof文件（redis性能、数据可控性适中） no 一旦aof缓冲区有数据时，就调用wirte命令往io缓冲区写数据。由操作系统定期或当io缓冲区填满时，自动将io缓冲区的数据写入到aof文件（redis性能最好，数据可控性最差） 总结：always只调用fsync，no只调用write，eversec既调用wirte，又每秒调用一次fsync</p><p><strong>write命令和fsync命令的区别：</strong></p><ul><li><p>wirte命令只能确定数据成功写入io缓冲区。数据是否成功写入aof文件，是由操作系统定期或当io缓冲区填满时，自动将io缓冲区的数据写入到aof文件。所以在io缓冲区的数据存在断电数据丢失的风险。</p></li><li><p>fsync命令具有强制将io缓冲区数据写入到aof文件的机制，可以确定数据是否成功写入aof文件。</p></li><li><p>java中write即write命令，flush即fsync命令。两个命令的使用是由于操作系统的机制决定的。</p></li></ul><p>使用 AOF 的优点是会让 Redis 变得非常耐久。可以设置不同的 Fsync 策略，AOF的默认策略是每秒钟 Fsync 一次，在这种配置下，就算发生故障停机，也最多丢失一秒钟的数据。</p><p>缺点是对于相同的数据集来说，AOF 的文件体积通常要大于 RDB 文件的体积。根据所使用的 Fsync 策略，AOF 的速度可能会慢于 RDB。</p><p>说了那么多，我们具体该使用哪一个呢？</p><p>无论是选择 方案还是技术时，抛开场景都是扯淡。所以如果你非常关心你的数据，但仍然可以承受数分钟内的数据丢失，那么可以只使用 RDB 持久。</p><p>AOF 将 Redis 执行的每一条命令追加到磁盘中，处理巨大的写入会降低Redis的性能，所以你为了追求数据的容灾和完整性的同时不知道你是否可以接受性能的下降。</p><p>数据库备份和灾难恢复：定时生成 RDB 快照非常便于进行数据库备份，并且 RDB 恢复数据集的速度也要比 AOF 恢复的速度快。</p><div class="language-java line-numbers-mode" data-ext="java"><pre class="language-java"><code>当然了，<span class="token class-name">Redis</span> 支持同时开启 <span class="token constant">RDB</span> 和 <span class="token constant">AOF</span>，系统重启后，<span class="token class-name">Redis</span> 会优先使用 <span class="token constant">AOF</span> 来恢复数据，这样丢失的数据会最少
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><div class="hint-container info"><p class="hint-container-title">相关信息</p><p>提示</p></div>`,29),p=[d];function l(c,o){return e(),s("div",null,p)}const t=a(i,[["render",l],["__file","test.html.vue"]]);export{t as default};
